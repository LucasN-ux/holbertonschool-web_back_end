#!/usr/bin/env python3
def floor(n: float) -> int:
    """Returns the floor of a float.

    Args:
        n: The float to be floored.

    Returns:
        The floor of the float.
    """
    return int(n)
